import random

# random.seed(1) # → força a buscar o mesmo número
# numero = random.randint(0, 10)  # → buscar números aleatórios


lista = [6, 45, 9]
numero = random.choice(lista) # → escolhe um número na lista

print(numero)
